{
  "name": "proxy-lists-upd",
  "version": "1.0.0",
  "description": "added API keys to some servers",
  "keywords": [
  ],
  "main": "index.js",
  "scripts": {
    "lint": "eslint --config .eslintrc.js *.js sources/*.js test/**/*.js",
    "prepublish": "npm run test # runs before `npm publish`",
    "pretest": "npm run lint # runs before `npm test`",
    "test": "mocha test/unit/ --recursive --reporter spec --ui bdd"
  },
  "bin": {
    "proxy-lists": "cli.js"
  },
  "repository": {

  },
  "author": {
  },
  "license": "MIT",
  "dependencies": {
    "async": "2.6.1",
    "cheerio": "1.0.0-rc.2",
    "commander": "2.19.0",
    "data-sourcer": "1.6.0",
    "geoip-native-lite": "0.3.0",
    "request": "2.88.0",
    "underscore": "1.9.1",
    "xml2js": "0.4.19"
  },
  "devDependencies": {
    "chai": "4.2.0"
  }
}
